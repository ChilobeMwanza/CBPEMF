
public interface DeltaResource 
{
	/*
	 * Optimise the output file by removing redundant changes. Essentially
	 * iterates the resource contents and persits them via the change based
	 * format.
	 */
	String OPTION_OPTIMISE_OUTPUT ="OPTIMISE_OUTPUT";
}
